import streamlit as st
import PyPDF2

st.set_page_config(page_title="JurisClear AI", page_icon="⚖️", layout="centered")

# --- Секция ссылок (ВСТАВЬ СВОИ ССЫЛКИ ИЗ LEMON SQUEEZY ТУТ) ---
link_9usd = "ВСТАВЬ_ССЫЛКУ_НА_ТОВАР_9_ДОЛЛАРОВ" 
link_29usd = "ВСТАВЬ_ССЫЛКУ_НА_ТОВАР_29_ДОЛЛАРОВ"
# -----------------------------------------------------------

st.title("⚖️ JurisClear AI")
st.subheader("Профессиональный ИИ-аудит юридических документов")

# Вкладки на главной странице
tab_main, tab_demo = st.tabs(["🚀 Начать анализ", "📝 Посмотреть пример отчета"])

with tab_main:
    st.write("### 1. Выберите тариф:")
    c1, c2 = st.columns(2)
    with c1:
        st.info("📄 **Разово**\n\n$9 / документ")
        st.link_button("Купить доступ", link_9usd, use_container_width=True)
    with c2:
        st.success("👑 **Pro**\n\n$29 / месяц")
        st.link_button("Оформить Pro", link_29usd, use_container_width=True)
    
    st.write("---")
    st.write("### 2. Загрузите файл:")
    uploaded_file = st.file_uploader("Загрузите договор в PDF", type="pdf")
    
    if uploaded_file:
        st.warning("🔒 Файл загружен. Для активации анализа ИИ требуется подтверждение оплаты (введите ваш API ключ в настройках, если вы владелец).")

with tab_demo:
    st.markdown("""
    ### Как выглядит готовый отчет:
    **Тип документа:** Трудовой договор.
    
    **⚠️ Критические находки:**
    1. **Пункт 5.4:** Ненормированный рабочий день без доплат.
    2. **Пункт 12.2:** Запрет на работу у конкурентов в течение 5 лет (слишком долгий срок, можно оспорить).
    
    **✅ Рекомендация:** Добавить пункт о компенсации за переработки.
    """)

st.divider()
st.caption("JurisClear AI | 2026 | Армения")
